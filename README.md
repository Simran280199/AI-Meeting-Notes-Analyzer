# AI-Meeting-Notes-Analyzer
An AI agent pipeline built with LangGraph and OpenAI GPT that analyzes meeting transcripts — extracting key topics, a summary, action items with owners, and an overall priority level. Includes a Streamlit web app for interactive use.
